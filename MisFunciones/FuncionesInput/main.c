#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
#include <string.h>
#include <ctype.h>
#include "FuncionesInput.h"
#define LARGO 20

int main(){


        /*strcpy(auxiliar,socioRecibido[j].apellido);
        strcpy(socioRecibido[j].apellido , socioRecibido[i].apellido);
        strcpy(socioRecibido[i].apellido , auxiliar);

        strcpy(auxiliar,socioRecibido[j].nombre);
        strcpy(socioRecibido[j].nombre , socioRecibido[i].nombre);
        strcpy(socioRecibido[i].nombre , auxiliar);

        strcpy(auxiliar,socioRecibido[j].email);
        strcpy(socioRecibido[j].email , socioRecibido[i].email);
        strcpy(socioRecibido[i].email , auxiliar);

        strcpy(auxiliar,socioRecibido[j].telefono);
        strcpy(socioRecibido[j].telefono , socioRecibido[i].telefono);
        strcpy(socioRecibido[i].telefono , auxiliar);

        socioRecibido[j].sexo = socioRecibido[i].sexo;
        socioRecibido[j].fechaSocio = socioRecibido[i].fechaSocio;
    */

return 0;
};
