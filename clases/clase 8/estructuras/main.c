#include <stdio.h>
#include "estructuras.h"
/*
struct persona {
char apellido[30];
char nombre[20];
int edad;
};
*/

//este anda

int main(void)
{
persona per;


per = alta(per);

printf("%d",per.edad);
printf("%s",per.apellido);


return 0;
};


